[project]
name = "radar-uaf"
version = "0.1.0"
description = "Radar OSINT de la Unidad de Analisis Financiero (UAF) de Chile, con enfoque AML/LA-FT"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
